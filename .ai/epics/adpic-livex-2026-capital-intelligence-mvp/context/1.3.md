# Context Brief — Stage 1.3 (epic: adpic-livex-2026-capital-intelligence-mvp)

Written by: ai-epic-own.sh (Epic Owner). Read this before starting the stage session.

## Prior-Stage Summary

### Prior completed stages (from epic state; lifecycle=done, proof=proven)

- (none completed yet — earlier stages: 1.1 (lifecycle=done), 1.2 (lifecycle=done))

### Declared dependencies (architecture "Depends On")

- 1.2: lifecycle=done

## Seams (must remain consistent)

```json
[]
```

## Must-Not-Break

- Do not change interfaces or outputs that prior done stages guarantee.
- Do not edit engine scripts/templates to force this stage forward (ownership boundary).
- Do not write epic state.json directly — use ai-epic-state.sh.

## Execution

- Execution transport (PROV-1): `durable_external` (external dispatch) — the Builder runs as a durable external dispatch; the independent Verifier is a separate actor.

## Deliverables

- Spec: `.ai/stages/1.3/spec.md` — the acceptance criteria + Success Proof the Builder must implement.
- Verification evidence: `.ai/stages/1.3/verify.sh` — the DC-1..DC-n observable checks that gate done; the Builder must make them pass.

## Verifier Contract

- The independent Verifier is a FRESH actor with no prior involvement: its identity is distinct from the Owner and the Builder, and it never continues the Builder session or reasoning.
- Reproduce the acceptance evidence by running the Success-Proof checks the spec declares (e.g. `bash .ai/stages/1.3/verify.sh`), reading the ACTUAL exit status and output, never a printed banner.
- Confirm every AC-n in `.ai/stages/1.3/spec.md` is satisfied by the delivered changeset, diffed against its own base via git merge-base, with nothing outside the declared scope.
- Attempt exactly the Owner-named adjacent bypass (bounded item), or verify adversarially and uncapped (chartered stage), per the dispatch package.
- Emit exactly one product verdict line: READY_FOR_GATE (all criteria met), REWORK_REQUIRED (with the specific failures), or BLOCKED (cannot verify).
<!-- generated-by: ai-epic-own.sh context; content-sha256: 5b40136fc19b1968a6bf19865c474a67065dc40df705ca70acc5a193be859301 -->
